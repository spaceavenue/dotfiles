#!/usr/bin/bash

color=$(
    slurp -b 00000000 -p \
    | grim -g - -t png - \
    | magick - -format '%[pixel:p{0,0}]' txt:- \
    | tail -n 1 \
    | cut -w -f 3
)
wl-copy -n $color;
notify-send -a "Color Picker" "Color $color copied to clipboard"
